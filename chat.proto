syntax = "proto3";

package chat;

option go_package = "github.com/CyberwizD/Everything-gRPC/chat";

message Chat {
    string body = 1;
}

service ChatService {
    rpc Response(Chat) returns (Chat) {}
}